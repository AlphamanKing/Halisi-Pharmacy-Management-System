// sales.js

// Add medication to cart during a sale
function addToCart(medicationId) {
    // Maintain a cart object (session-based or local storage)
}

// Process checkout and generate an invoice
function processCheckout(cartItems) {
    // Calculate total price, update inventory, and create an invoice record
}
